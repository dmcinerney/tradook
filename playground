import matplotlib
import cv2
import matplotlib.pyplot as plt
import numpy as np
import torch

# a = np.array([[8, 4], [2, 0], [4, 8], [10,6]])
# print a
# print np.min(a[:, 0])
# print np.min(a[:, 1])
# print np.max(a[:, 0])
# print np.max(a[:, 1])

a = torch.FloatTensor(5,7)
print a
a = a.view(a.numel())
print a
